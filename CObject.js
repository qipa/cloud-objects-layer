// Require col object
if (window && window.col) {

window.col.CObject = function _CObject () {
	// Pre-construct
	var CObject = this;
	
	// Object properties / methods
};

// Require col object
} else throw "!";
